import os
import time

def add-numbers(numbers):
  sum=0
  for num in numbers:
    sum += num
  return sum

def sort-numbers(numbers):
  sortedNumbers=[]
  #TODO: complete this
  
numbers = [21,43,54,6,67]

print(add-numbers(numbers))
